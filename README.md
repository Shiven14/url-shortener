# URL Shortener (AWS Free Tier)

Serverless URL shortener using **API Gateway (HTTP API) + Lambda + DynamoDB** and **AWS SAM**.

## Deploy

1) Prereqs: AWS account, AWS CLI, SAM CLI
2) Install deps:
```bash
npm ci
